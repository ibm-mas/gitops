
---
# IBM Cloud Pak Foundational Services
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ibm-db2u
  namespace: openshift-gitops
spec:
  goTemplate: true
  generators:
    - merge:
        mergeKeys:
          # Would love to use ".path.path" here to merge the configs together
          # but this is not possible currently with goTemplate: true - see https://github.com/argoproj/argo-cd/issues/12836
          # (and we really want goTemplate: true so we can dump the entire config file into HELM_VALUES using toPrettyJson)
          # instead, we are forced to explicitly provide a merge key in all of our config files to make them line up
          - 'merge-key'
        generators:
          # This is the "base" generator, it *must* be first in the list
          # (in this case, ibm-db2u is not an umbrella chart and we are not controlling its enablement
          # via Helm tags - instead we put the ibm-db2u.yaml config BEFORE ibm-mas-cluster-base.yaml
          # to ensure its only enabled once ibm-db2u.yaml is pushed)
          - git:
              repoURL: https://github.ibm.com/maximoappsuite/gitops-envs
              revision: mascore1032
              files:
              - path: "*/*/*/ibm-db2u.yaml"
           # The ordering of subsequent generators is unimportant
          - git:
              repoURL: https://github.ibm.com/maximoappsuite/gitops-envs
              revision: mascore1032
              files:
              - path: "*/*/*/ibm-mas-cluster-base.yaml"
  template:
    metadata:
      name: "ibm-db2u-{{ .account.id }}-{{ .region.id }}-{{ .cluster.id }}"
      labels:
        cloud: aws
        environment: '{{ .account.id }}'
        region: '{{ .region.id }}'
        cluster: '{{ .cluster.id }}'
    spec:
      project: mas
      sources:
        - repoURL: https://github.com/ibm-mas/gitops
          targetRevision: mascore1032
          path: applications/ibm-db2u
          plugin:
            env:
              - name: HELM_VALUES
                value: |
                  {{ toRawJson . }}
      destination:
        server: '{{ .cluster.url }}'
        namespace: 'default'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
