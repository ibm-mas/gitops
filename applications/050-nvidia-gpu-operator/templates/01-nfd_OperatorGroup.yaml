---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: "{{ .Values.nvidia_gpu_operator.nfd_namespace }}-group"
  namespace: "{{ .Values.nvidia_gpu_operator.nfd_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "051"
spec:
  targetNamespaces:
  - "{{ .Values.nvidia_gpu_operator.nfd_namespace }}"
