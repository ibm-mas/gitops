---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.ibm_sls.sls_mongo_secret_name }}"
  namespace: mas-{{ .Values.ibm_sls.mas_instance_id }}-sls
  annotations:
    argocd.argoproj.io/sync-wave: "103"
type: Opaque
stringData:
  username: "{{ .Values.ibm_sls.sls_mongo_username }}"
  password: "{{ .Values.ibm_sls.sls_mongo_password }}"
