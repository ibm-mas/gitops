{{- if eq .Values.mas_app_id "assist" }}
---
apiVersion: v1
data:
  wd.yaml: |
{{ .Values.ibm_suite_app_config.wd_yaml | toYaml | indent 4 }}
kind: ConfigMap
metadata:
  name: assist-watson-discovery-config
  namespace: "mas-{{ .Values.ibm_suite_app_config.mas_instance_id }}-assist"
  annotations:
    argocd.argoproj.io/sync-wave: "600"
{{- end }}
