{{- if and .Values.mas_manual_cert_mgmt (eq .Values.mas_manual_cert_mgmt true) }}
{{- if and .Values.public_tls_secret_name }}
---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.public_tls_secret_name }}"
  annotations:
    argocd.argoproj.io/sync-wave: "600"
data:
{{- if .Values.ca_crt }}
  ca.crt: {{ .Values.ca_crt }}
{{- end }}
{{- if .Values.tls_cert }}
  tls.crt: {{ .Values.tls_cert }}
{{- end }}
{{- if .Values.tls_key }}
  tls.key: {{ .Values.tls_key }}
{{- end }}

{{- end }}
{{- end }}
