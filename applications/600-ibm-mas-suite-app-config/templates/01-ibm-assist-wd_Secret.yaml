{{- if .Values.ibm_suite_app_config }}
{{- if eq .Values.ibm_suite_app_config.mas_app_id "assist" }}
---
apiVersion: v1
stringData:
  password: "{{ .Values.ibm_suite_app_config.cpd_admin_password }}"
  username: "{{ .Values.ibm_suite_app_config.cpd_admin_username }}"
kind: Secret
metadata:
  name: assist-watson-discovery-credentials
  namespace: "mas-{{ .Values.ibm_suite_app_config.mas_instance_id }}-assist"
  annotations:
    argocd.argoproj.io/sync-wave: "600"
type: Opaque
{{- end }}
{{- end }}
