---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: ibm-mas-operator-group
  namespace: "{{ .Values.ibm_db2u.db2_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "-7"
spec:
  targetNamespaces:
    - "{{ .Values.ibm_db2u.db2_namespace }}"
