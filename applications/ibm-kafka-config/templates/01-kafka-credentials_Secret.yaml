{{- if .Values.suite_kafka_username }}
---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.suite_kafka_secret_name }}" 
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  username: "{{ .Values.suite_kafka_username }}"
  password: "{{ .Values.suite_kafka_password }}"
{{- end }}
