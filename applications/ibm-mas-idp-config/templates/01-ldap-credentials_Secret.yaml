---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.suite_ldap_secret_name }}" 
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  username: "{{ .Values.suite_ldap_bind_dn }}"
  password: "{{ .Values.suite_smtp_bind_password }}"
