---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.spec.credentials.secretName }}"
  namespace: mas-{{ .Values.mas_instance_id }}-core
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  username: "{{ .Values.username }}"
  password: "{{ .Values.password }}"
