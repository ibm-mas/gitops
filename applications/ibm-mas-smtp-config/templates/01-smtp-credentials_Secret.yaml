---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.suite_smtp_secret_name }}" 
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  username: "{{ .Values.suite_smtp_username }}"
  password: "{{ .Values.suite_smtp_password }}"
