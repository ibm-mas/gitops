---
kind: Secret
apiVersion: v1
metadata:
   name: "{{ .Values.mas_instance_id }}-public-tls"
  annotations:
    argocd.argoproj.io/sync-wave: "-5"
  labels:
    test-me: "{{ .Values.tls_cert }}"
    test-me2: "{{ .Values.tls_key }}"
StringData:
  tls.crt: ""
  tls.key: ""
