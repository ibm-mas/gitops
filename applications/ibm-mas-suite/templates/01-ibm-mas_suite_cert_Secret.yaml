{{- if and .Values.core_crt .Values.mas_manual_cert_mgmt (eq .Values.mas_manual_cert_mgmt true) }}
---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.mas_instance_id }}-cert-public"
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
  labels:
    mas.ibm.com/instanceId: {{ .Values.mas_instance_id }}
data:
{{ .Values.core_crt }}
{{- end }}
