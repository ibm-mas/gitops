apiVersion: v2
name: ibm-mas-system-config-cleanup
description: Contains an ArgoCD PostSync hook to forcibly delete config CRs when their associated config files are removed from ArgoCD env config
type: application
version: 1.0.0
