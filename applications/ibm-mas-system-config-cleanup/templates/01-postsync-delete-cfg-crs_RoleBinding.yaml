---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: postsync-delete-cfg-crs-rb
  namespace: "mas-{{ .Values.instance.id }}-core"
  annotations:
    argocd.argoproj.io/sync-wave: "1"
subjects:
  - kind: ServiceAccount
    name: postsync-delete-cfg-crs-sa
    namespace: "mas-{{ .Values.instance.id }}-core"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: postsync-delete-cfg-crs-r
