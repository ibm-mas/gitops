---
apiVersion: core.mas.ibm.com/v1
kind: Workspace
metadata:
  name: "{{ .Values.ibm_mas_workspace.mas_instance_id }}-{{ .Values.ibm_mas_workspace.mas_workspace_id }}"
  namespace: mas-{{ .Values.ibm_mas_workspace.mas_instance_id }}-core
  labels:
    mas.ibm.com/instanceId: "{{ .Values.ibm_mas_workspace.mas_instance_id }}"
    mas.ibm.com/workspaceId: "{{ .Values.ibm_mas_workspace.mas_workspace_id }}"
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  displayName: "{{ .Values.ibm_mas_workspace.mas_workspace_name }}"
