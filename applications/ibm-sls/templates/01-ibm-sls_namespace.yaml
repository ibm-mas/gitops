---
apiVersion: project.openshift.io/v1
kind: Project
spec:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
  labels:
    app.kubernetes.io/instance: mas-{{ .Values.mas_instance_id }}-sls
spec:
  targetNamespaces:
    - mas-{{ .Values.mas_instance_id }}-sls