---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.mongo_spec.secretName }}"
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
type: Opaque
stringData:
  username: "{{ .Values.sls_mongo_username }}"
  password: "{{ .Values.sls_mongo_password }}"
