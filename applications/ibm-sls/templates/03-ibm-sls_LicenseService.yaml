---
apiVersion: sls.ibm.com/v1
kind: LicenseService
metadata:
  name: sls
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  mongo: {{ .Values.mongo_spec }}
  license:
    accept: true
  settings:
    auth:
      enforce: true
    registration:
      open: true
