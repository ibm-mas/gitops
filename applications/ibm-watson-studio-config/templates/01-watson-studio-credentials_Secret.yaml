{{- if .Values.cpd_admin_password }}
---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.mas_instance_id }}-usersupplied-watsonstudio-creds-system"
  annotations:
    argocd.argoproj.io/sync-wave: "0"
type: Opaque
stringData:
  username: "{{ .Values.suite_watson_studio_username }}"
  password: "{{ .Values.suite_watson_studio_password }}"
{{- end }}
