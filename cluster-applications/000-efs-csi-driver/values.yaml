---
efs_csi_driver:
  catalog_source: redhat-operators
  catalog_source_namespace: openshift-marketplace
  channel: stable
  subscription_source_namespace: openshift-cluster-csi-drivers
  role_arn: ""

# Storage class definitions for EFS
# This will be populated by the CLI function gitops_efs_csi_driver
# Example structure:
# storage_class_definitions:
#   efs-storage-class-name:
#     provisioner: efs.csi.aws.com
#     parameters:
#       provisioningMode: efs-ap
#       fileSystemId: fs-xxxxxxxxx
#       directoryPerms: "700"
storage_class_definitions: {}