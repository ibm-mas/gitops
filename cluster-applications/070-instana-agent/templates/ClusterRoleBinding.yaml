kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: monitor-cronjob-rolebinding
subjects:
  - kind: ServiceAccount
    name: cronjob
    namespace: "{{ .Values.instana_namespace }}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
