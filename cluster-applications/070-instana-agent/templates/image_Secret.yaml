kind: Secret
apiVersion: v1
metadata:
  name: image-secret
  namespace: "{{ .Values.instana_namespace }}"
  annotations:
    tekton.dev/docker-0: 'https://docker-na-public.artifactory.swg-devops.com'
data:
  .dockerconfigjson: |
    {{ .Values.instana_sre_ansible_image_pull_secret }}
type: kubernetes.io/dockerconfigjson
