kind: Secret
apiVersion: v1
metadata:
  name: instana-server-secret
  namespace: "{{ .Values.instana_namespace }}"
data:
  instana_nonprod.yml: >-
    {{ .Values.instana_nonprod }}
  instana_prod.yml: >-
    {{ .Values.instana_prod }}
type: Opaque
