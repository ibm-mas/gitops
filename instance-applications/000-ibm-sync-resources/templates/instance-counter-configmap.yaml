---
apiVersion: v1
kind: ConfigMap
metadata:
  name: instance-counter
  namespace: {{ .Values.argo.namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "998"  # Run before the counter job
data:
  count: "0"  # Initial count

# Made with Bob
