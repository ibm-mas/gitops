---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: "{{ .Values.cpd_service_subscription_name }}"
  namespace: "{{ .Values.cpd_operators_namespace }}"
spec:
  name: "{{ .Values.cpd_service_package_name }}"
  channel: "{{ .Values.cpd_service_channel_name }}"
  source: ibm-operator-catalog
  sourceNamespace: openshift-marketplace
  installPlanApproval: "{{ .Values.cpd_service_installplan_approval }}"
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ibm-cpd-ccs-operator
  namespace: "{{ .Values.cpd_operators_namespace }}"
spec:
  channel: "{{ .Values.ccs_channel }}"
  installPlanApproval: Automatic
  name: ibm-cpd-ccs
  source: ibm-operator-catalog
  sourceNamespace: openshift-marketplace 
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ibm-cpd-datarefinery-operator
  namespace: "{{ .Values.cpd_operators_namespace }}"
spec:
  channel: "{{ .Values.datarefinery_channel }}"
  installPlanApproval: Automatic
  name: ibm-cpd-datarefinery
  source: ibm-operator-catalog
  sourceNamespace: openshift-marketplace 
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ibm-cpd-wml-runtimes-operator
  namespace: "{{ .Values.cpd_operators_namespace }}"
spec:
  channel: "{{ .Values.wml_runtimes_channel }}"
  installPlanApproval: Automatic
  name: ibm-cpd-wml-runtimes
  source: ibm-operator-catalog
  sourceNamespace: openshift-marketplace 
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ibm-elasticsearch-operator-v1.1-ibm-operator-catalog-openshift-marketplace
  namespace: "{{ .Values.cpd_operators_namespace }}"
spec:
  channel: "{{ .Values.opencontent_elasticsearch_channel }}"
  installPlanApproval: Automatic
  name: ibm-elasticsearch-operator
  source: ibm-operator-catalog
  sourceNamespace: openshift-marketplace 
