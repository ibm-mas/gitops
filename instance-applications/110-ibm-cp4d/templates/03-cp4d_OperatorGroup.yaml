---
apiVersion: operators.coreos.com/v1alpha2
kind: OperatorGroup
metadata:
  name: common-service
  namespace: "{{ .Values.ibm_cp4d.cpd_operators_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "113"
spec:
  targetNamespaces:
    - "{{ .Values.ibm_cp4d.cpd_operators_namespace }}"


---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: ibm-licensing-operator-app
  namespace: "{{ .Values.ibm_cp4d.cpd_cs_control_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "113"
spec:
  targetNamespaces:
    - "{{ .Values.ibm_cp4d.cpd_cs_control_namespace }}"
  updateStrategy: Default
