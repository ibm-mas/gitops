---
apiVersion: cpd.ibm.com/v1
kind: Ibmcpd
metadata:
  name: "{{ .Values.ibm_cp4d.cpd_platform_cr_name }}"
  namespace: "{{ .Values.ibm_cp4d.cpd_instance_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "117"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  imagePullSecret: ibm_entitlement_key
  version: "{{ .Values.ibm_cp4d.cpd_product_version }}"
  license:
    accept: true
    license: Standard
  iamIntegration: {{ .Values.ibm_cp4d.cpd_iam_integration }}
  fileStorageClass: "{{ .Values.ibm_cp4d.cpd_primary_storage_class }}"
  blockStorageClass: "{{ .Values.ibm_cp4d.cpd_metadata_storage_class }}"
