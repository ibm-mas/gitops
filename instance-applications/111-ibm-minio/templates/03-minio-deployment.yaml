---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.minio_instanceName }}
  namespace: {{ .Values.minio_namespace }}
  annotations:
    argocd.argoproj.io/sync-wave: "113"
spec:
  selector:
    matchLabels:
      app: {{ .Values.minio_instanceName }}
  template:
    metadata:
      labels:
        app: {{ .Values.minio_instanceName }}
    spec:
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: minio-pvc
      containers:
        - name: {{ .Values.minio_instanceName }}
          image: {{ .Values.minio_image }}
          command:
            - /bin/bash
            - -c
          args:
            - minio server /data --console-address :9090
          env:
            - name: MINIO_ROOT_USER
              value: {{ .Values.minio_rootUser }}
            - name: MINIO_ROOT_PASSWORD
              value: {{ .Values.minio_rootPassword }}
          volumeMounts:
            - mountPath: /data
              name: storage