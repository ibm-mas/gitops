---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
    namespace: "{{ .values.service_mesh_namespace }}"
    package_name: "servicemeshoperator"
    package_channel: "{{ .values.service_mesh_channel }}"
    catalog_source: "{{ .values.service_mesh_catalog_source }}"
    annotations:
      argocd.argoproj.io/sync-wave: "116"
---
apiVersion: v1
metadata:
  name: istio-operator
  namespace: "{{ service_mesh_namespace }}"
  annotations:
      argocd.argoproj.io/sync-wave: "117"