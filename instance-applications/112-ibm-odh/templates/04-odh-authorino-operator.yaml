---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
    name: authorino-operator
    nnamespace: "{{ .Values.openshift_namespace }}"
    package_name: "authorino-operator"
    package_channel: "{{ .Values.serverless_channel }}"  # This looks wrong, but that's what was in the original template
    catalog_source: "{{ .Values.authorino_catalog_source }}"
    annotations:
      argocd.argoproj.io/sync-wave: "121"