---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.mas_aibroker_db_secret_name }}"
  namespace: "{{ .Values.aibroker_namespace }}"
  argocd.argoproj.io/sync-wave: "128"
type: Opaque
# stringData:
#   password: "{{ .Values.mas_aibroker_db_secret_value }}" # need to verify is it b64enc
data:
  password: {{ .Values.mas_aibroker_db_secret_value | b64enc }}
