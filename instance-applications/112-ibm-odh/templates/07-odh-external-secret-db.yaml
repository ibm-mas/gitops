---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.mas_aiservice_db_secret_name }}"
  namespace: "{{ .Values.aiservice_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "129"
type: Opaque
stringData:
  password: "{{ .Values.mas_aiservice_db_secret_value }}"
