---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.aiservice_s3_secret }}"
  namespace: "{{ .Values.aiservice_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "140"
stringData:
  access_key: "{{ .Values.aiservice_s3_accesskey}}"
  secret_key: "{{ .Values.aiservice_s3_secretkey}}"
type: Opaque