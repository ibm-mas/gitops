---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.mas_aiservice_mariadb_secret }}"
  namespace: "{{ .Values.aiservice_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "141"
stringData:
  username: "{{ .Values.mas_aiservice_db_user }}"
  password: "{{ .Values.mas_aiservice_db_secret_value }}"
type: Opaque
