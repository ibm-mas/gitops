---
kind: Secret
apiVersion: v1
metadata:
  name: "{{ .Values.mas_aibroker_sls_registration_key_secret }}"
  namespace:  "{{ .Values.aibroker_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "127"
data:
  SLS_REGISTRATION_KEY: "{{ .Values.slscfg.registration_key | b64encode }}" # how to inject here ?
type: Opaque