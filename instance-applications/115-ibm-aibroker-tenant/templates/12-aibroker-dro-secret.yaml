---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Values.tenantNamespace }}----dro-secret"
  namespace: "{{ .Values.aibroker_namespace }}"
type: Opaque
stringData:
  DRO_TOKEN: "{{ .Values.drocfg.registration_key }}"
