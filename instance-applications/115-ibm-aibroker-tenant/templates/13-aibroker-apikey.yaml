---
api_version: v1
kind: Secret
metadata:
  name: "{{ .Values.tenantNamespace }}----apikey-secret"
  namespace: "{{ .Values.aibroker_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "315"
type: Opaque
stringData:
  AIBROKER_APIKEY: {{ randAlphaNum 32 | b64enc | quote }}