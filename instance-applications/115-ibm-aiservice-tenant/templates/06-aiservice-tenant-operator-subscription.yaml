{{- if (not (hasPrefix "9.1." .Values.catalog_channel)) }}
---
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: "{{ .Values.tenantNamespace }}"
  namespace: "{{ .Values.tenantNamespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "306"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  targetNamespaces:
    - "{{ .Values.tenantNamespace }}"

---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: ibm-aiservice-tenant
  namespace: "{{ .Values.tenantNamespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "306"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
spec:
  channel: "{{ .Values.catalog_channel }}"
  installPlanApproval: Automatic
  name: ibm-aiservice-tenant
  source: "{{ .Values.catalog_source }}"
  sourceNamespace: openshift-marketplace
{{- end }}
