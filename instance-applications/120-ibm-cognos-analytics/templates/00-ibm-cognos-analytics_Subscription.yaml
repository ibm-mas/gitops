{{- if lt (semver "5.3.1" | (semver (.Values.cpd_product_version | toString)).Compare) 0 }}
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: cognos-analytics-subscription
  namespace: "{{ .Values.cpd_operators_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "092"
{{- if .Values.custom_labels }}
  labels:
{{ .Values.custom_labels | toYaml | indent 4 }}
{{- end }}
spec:
  channel: "{{ .Values.cognos_analytics_channel }}"
  name: cognos-analytics-operator
  source: ibm-operator-catalog
  sourceNamespace: openshift-marketplace
{{- end }}
