#apiVersion: batch/v1beta1
kind: CronJob
apiVersion: batch/v1
metadata:
  name: "masmsbackup-managedb-{{ .Values.db2_instance_name }}-db2u-0"
  namespace: "{{ .Values.db2_namespace }}"
  labels:
    parent: cronjob
    cosBucket: "{{ .Values.backup.bucket_name }}"
    db2pod: "c-{{ .Values.db2_instance_name }}-db2u-0"
spec:
  schedule: '0 2 * * *'
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            parent: cronjob
        spec:
          containers:
            - name: "masmsbackup-managedb-c-{{ .Values.db2_instance_name }}-db2u-0"
              image: openshift3/ose-ansible
              command:
                - oc
                - rsh
                - "c-{{ .Values.db2_instance_name }}-db2u-0"
                - /mnt/backup/bin/cronRunBKP.sh
                - "{{ .Values.backup.bucket_name }}"
              imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure
          serviceAccountName: "account-{{ .Values.db2_namespace }}-{{ .Values.db2_instance_name }}"
  successfulJobsHistoryLimit: 30
  failedJobsHistoryLimit: 10
