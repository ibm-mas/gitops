{{- if .Values.application_admin_role }}
{{- if and (eq .Values.mas_application_id "facilities") (not (contains "sdb" .Values.db2_instance_name)) }}

{{- /*
Use the build/bin/set-cli-image-digest.sh script to update this value across all charts.
*/}}
{{- $_cli_image_digest := "sha256:887cc3059a04601241687711df0c03d662931e6719121eec0eb0490001b2c8b9" }}

#
# CronJob for Volatile Tables RUNSTATS
# Runs every 10 minutes to update statistics for frequently changing tables
# Based on MASCORE-10348 - DB2 runstats required on MREF databases
#
kind: CronJob
apiVersion: batch/v1
metadata:
  name: "db2-volatile-runstats-{{ .Values.db2_instance_name }}"
  namespace: "{{ .Values.db2_namespace }}"
  labels:
    parent: cronjob
    db2pod: "c-{{ .Values.db2_instance_name }}-db2u-0"
    mas.ibm.com/applicationId: "{{ .Values.mas_application_id }}"
    mas.ibm.com/instanceId: "{{ .Values.instance_id }}"
  annotations:
    argocd.argoproj.io/sync-wave: "130"
spec:
  # Run every 10 minutes
  schedule: '*/10 * * * *'
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            parent: cronjob
        spec:
          containers:
            - name: "db2-volatile-runstats-{{ .Values.db2_instance_name }}"
              image: {{ .Values.cli_image_repo | default "quay.io/ibmmas/cli" }}@{{ $_cli_image_digest }}
              command:
                - oc
                - rsh
                - "c-{{ .Values.db2_instance_name }}-db2u-0"
                - su
                - -lc
                - {{ .Values.db2_instance_home_path }}/bin/runstats_volatile.sh
                - db2inst1
              imagePullPolicy: IfNotPresent
          restartPolicy: OnFailure
          serviceAccountName: "account-{{ .Values.db2_namespace }}-{{ .Values.db2_instance_name }}"
  successfulJobsHistoryLimit: 30
  failedJobsHistoryLimit: 10
{{- end }}
{{- end }}
