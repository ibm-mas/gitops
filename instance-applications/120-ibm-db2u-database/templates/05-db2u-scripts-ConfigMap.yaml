apiVersion: v1
kind: ConfigMap
metadata:
  name: db2u-scripts-{{ .Values.db2_instance_name }}-configmap
  namespace: "{{ .Values.db2_namespace }}"
data:
{{ (.Files.Glob "files/*").AsConfig | indent 2 }}

# binaryData:
#   db2shc: |-
#     {{ .Files.Get "binary_files/db2shc" | b64enc }}
