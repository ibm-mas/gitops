{{- if contains "sdb" .Values.db2_instance_name }}
apiVersion: addons.mas.ibm.com/v1
kind: ReplicaDB
metadata:
  name: "{{ .Values.mas_application_id }}ReplicaDb"
  namespace: "{{ .Values.db2_namespace }}"
spec:
  displayName: "{{ .Values.mas_application_id }}ReplicaDb"
  config:
    instances:
      - name: "{{ .Values.mas_application_id }}ReplicaDb"
{{- end }}