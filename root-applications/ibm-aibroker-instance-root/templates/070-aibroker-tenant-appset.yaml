---
# IBM AiBroker Tenant Application Set
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: aibroker-tenant-appset.{{ .Values.cluster.id }}.{{ .Values.instance.id }}
  namespace: {{ .Values.argo.namespace }}
  labels:
    environment: '{{ .Values.account.id }}'
    cluster: '{{ .Values.cluster.id }}'
    instance: '{{ .Values.instance.id }}'
    tenant: '{{ .Values.tenant.id }}'
  annotations:
    argocd.argoproj.io/sync-wave: "070"
spec:
  goTemplate: true
  generators:
    - git:
        repoURL: "{{ .Values.generator.repo_url }}"
        revision: "{{ .Values.generator.revision }}"
        files:
          - path: "{{ .Values.account.id }}/{{ .Values.cluster.id }}/{{ .Values.instance.id }}/aibroker-tenant01/ibm-aibroker-tenant.yaml"
  template:
    metadata:
      name: "aibroker-tenant.{{.cluster_id }}.{{.instance_id }}"
      labels:
        environment: '{{.environment }}'
        cluster: '{{.cluster_id }}'
        instance: '{{.instance_id }}'
{{- if .Values.custom_labels }}
{{ .Values.custom_labels | toYaml | indent 8 }}
{{- end }}
      annotations:
        healthCheckTimeout: "1800"
        argocd.argoproj.io/sync-wave: "070"
{{- if and .Values.notifications .Values.notifications.slack_channel_id }}
        notifications.argoproj.io/subscribe.on-sync-failed.workspace1: {{ .Values.notifications.slack_channel_id }}
        notifications.argoproj.io/subscribe.on-sync-succeeded.workspace1: {{ .Values.notifications.slack_channel_id }}
{{- end }}
    spec:
      project: "{{ .Values.argo.projects.rootapps }}"
      source:
        repoURL: "{{ .Values.source.repo_url }}"
        targetRevision: "{{ .Values.source.revision }}"
        path: instance-applications/115-ibm-aibroker-tenant
        plugin:
          name: {{ .Values.avp.name }}
          env:
            - name: {{ .Values.avp.values_varname }}
              value: |
                mas_instance_id: "{{ .mas_instance_id }}"
                aibroker_namespace: "{{ .aibroker_namespace }}"
                junitreporter:
                  reporter_name: "ibm-aibroker-tenant-{{ .instance_id }}"
                  cluster_id: "{{ .cluster_id }}"
                  devops_mongo_uri: "{{ .devops_mongo_uri }}"
                  devops_build_number: "{{ .devops_build_number }}"
                  gitops_version: "{{ .gitops_version }}"
            - name: ARGOCD_APP_NAME
              value: aibrokertenantapp
{{- if not (empty .Values.avp.secret) }}
            - name: AVP_SECRET
              value: {{ .Values.avp.secret }}
{{- end }}
      destination:
        server: https://kubernetes.default.svc
        namespace: {{ .aibroker_namespace }}
      syncPolicy:
        automated:
{{- if .Values.auto_delete }}
          prune: true
{{- end }}
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - RespectIgnoreDifferences=true
        retry:
          limit: -1
      ignoreDifferences:
        - group: '*'
          kind: ServiceAccount
          jsonPointers:
            - /imagePullSecrets
        - group: 'marketplace.redhat.com/v1alpha1'
          kind: MarketplaceConfig
          jsonPointers:
            - /spec
      revisionHistoryLimit: 1
