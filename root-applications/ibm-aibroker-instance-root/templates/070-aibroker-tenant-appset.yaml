
---
# IBM AiBroker Tenant Application Set
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: aibroker-tenant-appset.{{ .Values.cluster.id }}.{{ .Values.instance.id }}
  namespace: {{ .Values.argo.namespace }}
  labels:
    environment: '{{ .Values.account.id }}'
    cluster: '{{ .Values.cluster.id }}'
    instance: '{{ .Values.instance.id}}'
  annotations:
    argocd.argoproj.io/sync-wave: "070"
spec:
  goTemplate: true
  generators:
    - merge:
        mergeKeys:
          # Would love to use ".path.path" here to merge the configs together
          # but this is not possible currently with goTemplate: true - see https://github.com/argoproj/argo-cd/issues/12836
          # (and we really want goTemplate: true so we can dump the entire config file into HELM_VALUES using toPrettyJson)
          # instead, we are forced to explicitly provide a merge key in all of our config files to make them line up
          - 'merge-key'
        generators:
          # This is the "base" generator, it *must* be first in the list
          - git:
              repoURL: "{{ .Values.generator.repo_url }}"
              revision: "{{ .Values.generator.revision }}"
              files:
              - path: "{{ .Values.account.id }}/{{ .Values.cluster.id }}/{{ .Values.instance.id }}/*/ibm-aibroker-tenant-base.yaml"
          - git:
              repoURL: "{{ .Values.generator.repo_url }}"
              revision: "{{ .Values.generator.revision }}"
              files:
              - path: "{{ .Values.account.id }}/{{ .Values.cluster.id }}/{{ .Values.instance.id }}/*/ibm-aibroker-tenant.yaml"                 
  syncPolicy:
    applicationsSync: "{{- if .Values.auto_delete }}sync{{- else }}create-update{{- end }}"
  template:
    metadata:
      name: "aibroker-tenant.{{ .Values.cluster.id }}.{{ .Values.instance.id }}.{{ `{{.tenant.id}}` }}"
      labels:
        environment: '{{ .Values.account.id }}'
        cluster: '{{ .Values.cluster.id }}'
        instance: '{{ .Values.instance.id }}'
        tenant: '{{ `{{ .tenant.id }}` }}'
{{- if .Values.custom_labels }}
{{ .Values.custom_labels | toYaml | indent 8 }}
{{- end }}
      annotations:
        healthCheckTimeout: "1800"
        argocd.argoproj.io/sync-wave: "070"
        {{- if and .Values.notifications .Values.notifications.slack_channel_id }}
        notifications.argoproj.io/subscribe.on-sync-failed.workspace1: {{ .Values.notifications.slack_channel_id }}
        notifications.argoproj.io/subscribe.on-sync-succeeded.workspace1: {{ .Values.notifications.slack_channel_id }}
        {{- end }}
    spec:
      project: "{{ .Values.argo.projects.rootapps }}"
      source:
          repoURL: "{{ .Values.source.repo_url }}"
          targetRevision: "{{ .Values.source.revision }}"
          path: instance-applications/115-ibm-aibroker-tenant
          plugin:
            name: {{ .Values.avp.name }} 
            env:
              - name: {{ .Values.avp.values_varname }}
                value: |
                  tenant_id: "{{ .ibm_aibroker_tenant.tenant_id }}"
                  mas_instance_id: "{{ .ibm_aibroker_tenant.mas_instance_id }}"
                  aibroker_namespace: "mas-{{ .ibm_aibroker_tenant.mas_instance_id }}-aibroker"
                  ibm_entitlement_key: "{{ .ibm_aibroker_tenant.ibm_entitlement_key }}"
                  aibroker_saas_apikey: "{{ .ibm_aibroker_tenant.aibroker_saas_apikey }}"
                  mas_aibroker_saas: "{{ .ibm_aibroker_tenant.mas_aibroker_saas }}"
                  mas_aibroker_provision_tenant: "{{ .ibm_aibroker_tenant.mas_aibroker_provision_tenant }}"
                  artifactory_token: "{{ .ibm_aibroker_tenant.artifactory_token }}"
                  mas_aibroker_tenant_name: "{{ .ibm_aibroker_tenant.mas_aibroker_tenant_name }}"
                  tenant_action: "{{ .ibm_aibroker_tenant.tenant_action }}"
                  tenantNamespace: "aibroker-{{ .ibm_aibroker_tenant.mas_aibroker_tenant_name }}"
                  mas_icr_cp: "{{ .ibm_aibroker_tenant.mas_icr_cp }}"
                  mas_icr_cpopen: "{{ .ibm_aibroker_tenant.mas_icr_cpopen }}"
                  mas_aibroker_dro_cacert_secret: "{{ .ibm_aibroker_tenant.mas_aibroker_dro_cacert_secret }}"
                  drocfg_ca: "{{ .ibm_aibroker_tenant.drocfg_ca }}"
                  drocfg_registration_key: "{{ .ibm_aibroker_tenant.drocfg_registration_key }}"
                  drocfg_url: "{{ .ibm_aibroker_tenant.drocfg_url }}"
                  slscfg_registration_key: "{{ .ibm_aibroker_tenant.slscfg_registration_key }}"
                  slscfg_url: "{{ .ibm_aibroker_tenant.slscfg_url }}"
                  mas_aibroker_watsonxai_apikey: "{{ .ibm_aibroker_tenant.mas_aibroker_watsonxai_apikey }}"
                  mas_aibroker_watsonxai_url: "{{ .ibm_aibroker_tenant.mas_aibroker_watsonxai_url }}"
                  mas_aibroker_watsonxai_project_id: "{{ .ibm_aibroker_tenant.mas_aibroker_watsonxai_project_id }}"
                  mas_aibroker_sls_subscription_id: "{{ .ibm_aibroker_tenant.mas_aibroker_sls_subscription_id }}"
                  rsl_url: "{{ .ibm_aibroker_tenant.rsl_url }}"
                  rsl_org_id: "{{ .ibm_aibroker_tenant.rsl_org_id }}"
                  rsl_token: "{{ .ibm_aibroker_tenant.rsl_token }}"
                  mas_aibroker_storage_provider: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_provider }}"
                  mas_aibroker_storage_ssl: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_ssl }}"
                  mas_aibroker_storage_accesskey: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_accesskey }}"
                  mas_aibroker_storage_secretkey: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_secretkey }}"
                  mas_aibroker_storage_host: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_host }}"
                  mas_aibroker_storage_port: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_port }}"
                  mas_aibroker_storage_region: "{{ .ibm_aibroker_tenant.mas_aibroker_storage_region }}"
                  mas_aibroker_s3_endpoint_url: "{{ .ibm_aibroker_tenant.mas_aibroker_s3_endpoint_url }}"
                  mas_aibroker_s3_region: "{{ .ibm_aibroker_tenant.mas_aibroker_s3_region }}"
                  mas_aibroker_s3_bucket_prefix: "{{ .ibm_aibroker_tenant.mas_aibroker_s3_bucket_prefix }}"
                  tenant_entitlement_type: "{{ .ibm_aibroker_tenant.tenant_entitlement_type }}"
                  tenant_entitlement_start_date: "{{ .ibm_aibroker_tenant.tenant_entitlement_start_date }}"
                  tenant_entitlement_end_date: "{{ .ibm_aibroker_tenant.tenant_entitlement_end_date }}"
                  junitreporter:
                    reporter_name: "ibm-aibroker-tenant-{{ .ibm_aibroker_tenant.mas_instance_id }}"
                    cluster_id: "{{ .cluster.id }}"
                    devops_mongo_uri: "{{ .devops.mongo_uri }}"
                    devops_build_number: "{{ .devops.build_number }}"
                    gitops_version: "{{ .source.revision }}"
              - name: ARGOCD_APP_NAME
                value: aibrokertenantapp
              {{- if not (empty .Values.avp.secret) }}
              - name: AVP_SECRET
                value: {{ .Values.avp.secret }}
              {{- end }}
      destination:
        server: 'https://kubernetes.default.svc'
        namespace: {{ .Values.argo.namespace }}
      syncPolicy:
        automated:
          {{- if .Values.auto_delete }}
          prune: true
          {{- end }}
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
          - RespectIgnoreDifferences=true
        retry:
          limit: -1
      ignoreDifferences:
        - group: '*'
          kind: ServiceAccount
          jsonPointers:
            - /imagePullSecrets
        - group: 'marketplace.redhat.com/v1alpha1'
          kind: MarketplaceConfig
          jsonPointers:
            - /spec
      revisionHistoryLimit: 1