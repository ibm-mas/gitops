{{- if not (empty (index .Values "ibm-dro")) }}
---
# IBM Maximo Operator Catalog
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dro.{{.Values.account.id}}.{{.Values.region.id}}.{{ .Values.cluster.id }}
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  ignoreDifferences:
  - group: 'marketplace.redhat.com/v1alpha1'
    kind: MarketplaceConfig
    jsonPointers:
    - /spec
  project: mas
  destination:
    server: {{ .Values.cluster.url }}
    namespace: default
  source:
    repoURL: https://github.com/ibm-mas/gitops
    path: applications/ibm-dro
    targetRevision: mascore1653
    plugin:
      env:
        - name: HELM_VALUES
          # This copies keys under "ibm-dro" to the root config level so we don't have to prefix
          # all references to ibm-dro config with ibm-dro in the application chart,
          # while still passing in the values from cluster-base (e.g. account.id etc)
          # which are now needed by the postsync jobs (see https://jsw.ibm.com/browse/MASCORE-1425)
          # TODO: Probably a better way of handling this
          # TODO: what if there's a clash between a root prop and one defined under ibm-dro?
          # TODO: Probably don't actually want to "pollute" our externally-available, pipeline-agnostic
          #       application charts with MaS-SaaS-specific stuff (like the postsync jobs) anyway
          value: |
            {{ mustMergeOverwrite .Values (index .Values "ibm-dro") | toRawJson }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
{{- end }}