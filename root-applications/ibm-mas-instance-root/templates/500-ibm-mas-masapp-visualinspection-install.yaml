{{- if not (empty .Values.ibm_mas_masapp_visualinspection_install) }}
---
# IBM Maximo Operator Catalog
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: visualinspection.{{.Values.account.id}}.{{.Values.region.id}}.{{ .Values.cluster.id }}.{{ .Values.instance.id }}
  namespace: {{ .Values.argoapp_namespace }}
  labels:
    cloud: aws
    environment: '{{ .Values.account.id }}'
    region: '{{ .Values.region.id }}'
    cluster: '{{ .Values.cluster.id }}'
    instance: '{{ .Values.instance.id }}'
    masapp: 'visualinspection'
  annotations:
    argocd.argoproj.io/sync-wave: "500"
    {{- if and .Values.notifications .Values.notifications.slack_channel_id }}
    notifications.argoproj.io/subscribe.on-sync-failed.slack: {{ .Values.notifications.slack_channel_id }}
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: {{ .Values.notifications.slack_channel_id }}
    {{- end }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: mas
  destination:
    server: {{ .Values.cluster.url }}
    namespace: default
  source:
    repoURL: https://github.com/ibm-mas/gitops
    targetRevision: "{{ .Values.source.targetRevision }}"
    path: applications/500-ibm-mas-masapp-install-stub
    plugin:
      env:
        - name: HELM_VALUES
          value: |
            {{ unset (set .Values "ibm_suite_app_install" .Values.ibm_suite_app_visualinspection_install) "ibm_suite_app_visualinspection_install" | toRawJson }}
        - name: ARGOCD_APP_NAME
          value: "visualinspection-install"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
{{- end }}
