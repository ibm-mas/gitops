{{- if not (empty (index .Values "bindings") or (index .Values "bindings" "system") ) }}
---
# IBM Maximo Operator Catalog
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: system-bindings.{{.Values.account.id}}.{{.Values.region.id}}.{{ .Values.cluster.id }}.{{ .Values.instance.id }}
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  ignoreDifferences:
  - group: '*'
    kind: ServiceAccount
    jsonPointers:
    - /imagePullSecrets
  project: mas
  destination:
    server: {{ .Values.cluster.url }}
    namespace: default
  sources:
    {{- if not (empty (index .Values "bindings" "system" "ibm-mas-mongo-config")) }}
    - path: applications/ibm-mas-mongo-config
      repoURL: https://github.com/ibm-mas/gitops
      targetRevision: mascore1032v2
      plugin:
        env:
          - name: HELM_VALUES
            value: |
              {{ index .Values "bindings" "system" "ibm-mas-mongo-config" | toRawJson }}
    {{- end }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
{{- end }}