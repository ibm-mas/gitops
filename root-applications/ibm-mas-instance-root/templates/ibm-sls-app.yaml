{{- if not (empty (index .Values "ibm-sls")) }}
---
# IBM Maximo Operator Catalog
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sls.{{.Values.account.id}}.{{.Values.region.id}}.{{ .Values.cluster.id }}.{{ .Values.instance.id }}
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  ignoreDifferences:
  - group: '*'
    kind: ServiceAccount
    jsonPointers:
    - /imagePullSecrets
  project: mas
  destination:
    server: {{ .Values.cluster.url }}
    namespace: default
  source:
    repoURL: https://github.com/ibm-mas/gitops
    path: applications/ibm-sls
    targetRevision: mascore1653
    plugin:
      env:
        - name: HELM_VALUES
          # This copies keys under "ibm-sls" to the root config level so we don't have to prefix
          # all references to ibm-sls config with ibm-sls in the application chart,
          # while still passing in the values from cluster-base (e.g. account.id etc)
          # which are now needed by the postsync jobs (see https://jsw.ibm.com/browse/MASCORE-1425)
          # TODO: Probably a better way of handling this
          # TODO: what if there's a clash between a root prop and one defined under ibm-sls?
          # TODO: Probably don't actually want to "pollute" our externally-available, pipeline-agnostic
          #       application charts with MaS-SaaS-specific stuff (like the postsync jobs) anyway
          value: |
            {{ mustMergeOverwrite .Values (index .Values "ibm-sls") | toRawJson }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
      - RespectIgnoreDifferences=true
{{- end }}