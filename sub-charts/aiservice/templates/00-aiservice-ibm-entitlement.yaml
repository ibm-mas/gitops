---
apiVersion: v1
kind: Secret
metadata:
  name: ibm-entitlement
  namespace: "{{ global.Values.aiservice_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "146"
    namespace.value: "{{ global.Values.aiservice_namespace }}"
    dockerconfig.value: "{{ global.Values.artifactory_token }}"
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "{{ global.Values.artifactory_token }}"