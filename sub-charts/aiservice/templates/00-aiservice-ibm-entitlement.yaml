---
apiVersion: v1
kind: Secret
metadata:
  name: ibm-entitlement
  namespace: {{ tpl .Values.app_namespace . | quote }}
  annotations:
    argocd.argoproj.io/sync-wave: "146"
    token: "{{ .Values.ibm_aiservice.artifactory_token }}"
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "{{ .Values.ibm_aiservice.artifactory_token }}"