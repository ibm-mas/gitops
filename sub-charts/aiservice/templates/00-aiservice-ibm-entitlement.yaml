---
apiVersion: v1
kind: Secret
metadata:
  name: ibm-entitlement
  namespace: "{{ .Values.aiservice_namespace }}"
  annotations:
    argocd.argoproj.io/sync-wave: "146"
    aiservice_namespace: "{{ .Values.aiservice_namespace }}"
    aiservice_artifactory_token: "{{ .Values.artifactory_token }}"
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: "{{ .Values.artifactory_token }}"