apiVersion: v2
name: fvt-gitops-runner
description: |
  Helm chart that runs the fvt-gitops test suite as a PostSync Kubernetes Job.
  Intended to be included as a sub-chart in GitOps ArgoCD applications.
type: application
version: 1.0.0
