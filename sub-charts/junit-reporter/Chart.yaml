apiVersion: v2
name: junit-reporter
description: |
  Updates the devops database when a argo app is first synched and then marks it complete
  along with a junit xml test result.
type: application
version: 1.0.0
